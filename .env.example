# ===================================================================
#         EXAMPLE FILE FOR THE NODEHUB PROJECT (MERAKI)
# ===================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named ".env"
# 2. The ".env" file is SECRET and MUST NOT be committed to Git (it is already in .gitignore).
# 3. Fill in the values below, especially the password.
#
# ===================================================================


# --- 1. DATABASE CONFIGURATION (Read by Docker Compose) ---
#
# User that Postgres will create
POSTGRES_USER=nodehub_user

# SECRET PASSWORD: Choose a strong, unique password for your local database.
POSTGRES_PASSWORD=put_your_secret_password_here

# Name of the database that will be created
POSTGRES_DB=nodehub_db

# Port that the database will be exposed on your PC (localhost)
# 5432 is the Postgres default.
POSTGRES_PORT=5432


# --- 2. RUST API CONFIGURATION (Read by Axum/SQLx) ---
#
# This URL is assembled using the variables above.
# Rust (on WSL) uses 'host.docker.internal' to reach Docker (running on Windows).
#
# **DO NOT CHANGE THIS LINE**, it will read the variables above automatically.
#
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@host.docker.internal:${POSTGRES_PORT}/${POSTGRES_DB}"